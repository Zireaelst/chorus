import type { Metadata } from 'next'

// apps/dashboard home page — Sprint 0 scaffold
// Authentication wall and hospital portal built in v0.3 and v0.4.
export const metadata: Metadata = {
  title: 'Chorus Dashboard — Hospital Operator',
}

export default function DashboardPage() {
  return null
}
