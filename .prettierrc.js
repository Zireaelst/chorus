// Prettier config — delegates to @chorus/config
// CODING_STANDARDS.md: "Prettier configs live in packages/config and are never overridden per-app"
const config = require('@chorus/config/prettier')
module.exports = config
